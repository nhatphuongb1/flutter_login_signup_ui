import 'package:flutter/material.dart';

class WorkThroughItem extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: <Widget>[
          Container(
            child: Text("Title"),
          )
        ],
      ),
    );
  }
}
